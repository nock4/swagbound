# FontStruct Earthbound tag fonts

Downloaded from https://fontstruct.com/gallery/tag/5492/Earthbound.

Archives are in `archives/`; extracted files are in `otf/`. Filenames are prefixed with the FontStruct ID to avoid duplicate-name collisions. See `manifest.json` for source URLs and license links captured from the gallery page.
