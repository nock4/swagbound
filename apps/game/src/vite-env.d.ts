/// <reference types="vite/client" />

// Injected by vite define: short git sha + dev-server start time (the build stamp
// shown in the debug HUD so evidence screenshots prove which build they ran on).
declare const __BUILD_STAMP__: string;
